# This environment can be used to install cebra

name: cebra
channels:
    - pytorch
    - defaults
dependencies:
    - python<3.10
    - pip
    - pytorch
    - jupyter
    - nb_conda
    - pip:
        - cebra
